"use client";
import Link from "next/link";import{useEffect,useState}from"react";import{ArrowDownRight,ArrowUpRight,BookOpen,IndianRupee,Loader2,ReceiptIndianRupee,Scale,WalletCards}from"lucide-react";import{Bar,BarChart,CartesianGrid,Legend,ResponsiveContainer,Tooltip,XAxis,YAxis}from"recharts";
type Summary={academicYear:string;feeExpected:number;totalCollected:number;totalExpenses:number;netProfitLoss:number;totalIncome:number;totalExpenditure:number;incomeStreams:{feeCollections:number;cashBookIncome:number;otherIncome:number;grandTotal:number};expenditureStreams:{staffSalaries:number;cashBookExpenses:number;otherExpenses:number;grandTotal:number};monthly:{key:string;label:string;income:number;expenses:number;net:number;feeCollections:number;staffSalaries:number;otherIncome:number;otherExpenses:number}[]};
const money=(p:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(p/100);
export function AccountsDashboard(){const[data,setData]=useState<Summary|null>(null),[years,setYears]=useState<string[]>([]),[year,setYear]=useState(""),[active,setActive]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{setLoading(true);fetch(`/api/accounts${year?`?year=${year}`:""}`).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j.summary);setYears(j.academicYears);setActive(j.activeAcademicYear);if(!year)setYear(j.summary.academicYear)}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[year]);if(loading&&!data)return <div className="grid min-h-[60vh] place-items-center"><Loader2 className="animate-spin text-violet-600"/></div>;if(!data)return <p className="card p-6 text-red-600">{error}</p>;const cards=[["Total Fee Expected",data.feeExpected,ReceiptIndianRupee,"text-violet-600 bg-violet-100"],["Total Collected",data.totalCollected,ArrowUpRight,"text-emerald-600 bg-emerald-100"],["Total Expenses",data.totalExpenses,ArrowDownRight,"text-red-600 bg-red-100"],["Net Profit / Loss",data.netProfitLoss,Scale,data.netProfitLoss<0?"text-red-600 bg-red-100":"text-emerald-600 bg-emerald-100"]] as const;return <div className="mx-auto max-w-[1500px]"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="label">Finance · Live ledger</p><h1 className="mt-2 text-3xl font-black sm:text-4xl">Accounts Dashboard</h1><p className="mt-2 text-muted">Fee collections, income, expenditure and financial growth from verified records.</p></div><div className="flex flex-col gap-2 sm:flex-row"><label className="text-xs font-bold text-muted">Academic year<select value={year} onChange={e=>setYear(e.target.value)} className="mt-1 block w-full min-w-48 rounded-xl border bg-panel px-4 py-3 text-sm font-bold text-ink">{years.map(y=><option key={y} value={y}>{y}{y===active?" (Active)":""}</option>)}</select></label><Link href="/dashboard/cash-book" className="btn-primary self-end"><BookOpen className="size-4"/>Open Cash Book</Link></div></div>{error&&<p className="mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</p>}
<section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label,value,Icon,color])=><article className="card p-5 transition hover:-translate-y-0.5 hover:shadow-lg" key={label}><div className="flex items-start justify-between"><span className={`grid size-11 place-items-center rounded-2xl ${color}`}><Icon className="size-5"/></span><span className="rounded-full bg-canvas px-2 py-1 text-[10px] font-bold text-muted">{year}</span></div><b className={`mt-6 block text-2xl sm:text-3xl ${label==="Net Profit / Loss"?(value<0?"text-red-600":"text-emerald-600"):""}`}>{money(value)}</b><p className="mt-1 text-sm text-muted">{label}</p></article>)}</section>
<section className="card mt-5 p-5 sm:p-6"><div><h2 className="text-lg font-bold">Financial Growth Timeline</h2><p className="text-sm text-muted">Monthly income and expenses for {year}</p></div><div className="mt-6 h-80 min-w-0"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.monthly.map(x=>({...x,income:x.income/100,expenses:x.expenses/100,net:x.net/100}))} margin={{left:0,right:5}}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="label" tick={{fontSize:11}}/><YAxis tick={{fontSize:11}} tickFormatter={v=>`₹${Number(v)/1000}k`}/><Tooltip formatter={(v)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(Number(v))}/><Legend/><Bar dataKey="income" name="Income" fill="#10b981" radius={[5,5,0,0]}/><Bar dataKey="expenses" name="Expenses" fill="#ef4444" radius={[5,5,0,0]}/><Bar dataKey="net" name="Net" fill="#7c3aed" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></section>
<div className="mt-5 grid gap-5 lg:grid-cols-2"><Stream title="Income Streams" icon={IndianRupee} tone="text-emerald-600" rows={[["Fee Collections",data.incomeStreams.feeCollections],["Cash Book Income",data.incomeStreams.cashBookIncome],["Other Income",data.incomeStreams.otherIncome],["Grand Total Income",data.incomeStreams.grandTotal]]}/><Stream title="Expenditure Streams" icon={WalletCards} tone="text-red-600" rows={[["Staff Salaries",data.expenditureStreams.staffSalaries],["Cash Book Expenses",data.expenditureStreams.cashBookExpenses],["Other Expenses",data.expenditureStreams.otherExpenses],["Grand Total Expenses",data.expenditureStreams.grandTotal]]}/></div></div>}
function Stream({title,icon:Icon,tone,rows}:{title:string;icon:typeof IndianRupee;tone:string;rows:[string,number][]}){return <section className="card p-6"><div className="flex items-center gap-3"><span className={`grid size-10 place-items-center rounded-xl bg-canvas ${tone}`}><Icon className="size-5"/></span><h2 className="text-lg font-bold">{title}</h2></div><div className="mt-5 divide-y">{rows.map(([label,value],i)=><div className={`flex justify-between gap-4 py-4 ${i===rows.length-1?"text-base font-black":"text-sm"}`} key={label}><span className={i===rows.length-1?"":"text-muted"}>{label}</span><b className={i===rows.length-1?tone:""}>{money(value)}</b></div>)}</div></section>}
