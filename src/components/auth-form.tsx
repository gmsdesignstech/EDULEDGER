"use client";
import Link from"next/link";import{useRouter}from"next/navigation";import{useState}from"react";import{Eye,EyeOff,Loader2,ShieldCheck}from"lucide-react";import{Logo}from"./logo";
export function AuthForm({mode}:{mode:"login"|"register"}){const router=useRouter();const[show,setShow]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState("");
 async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setError("");const form=new FormData(event.currentTarget),password=String(form.get("password")??"");if(mode==="register"&&password!==String(form.get("confirmPassword")??"")){setError("Passwords do not match.");return}setLoading(true);try{const response=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),institution:form.get("institution"),email:form.get("email"),password})});const result=await response.json()as{error?:string};if(!response.ok)throw new Error(result.error??"Authentication failed.");router.replace(mode==="register"?`/subscription${window.location.search}`:"/dashboard");router.refresh()}catch(reason){setError(reason instanceof Error?reason.message:"Something went wrong. Please try again.");setLoading(false)}}
 return <main className="grid min-h-screen lg:grid-cols-2"><section className="hidden mesh grid-bg place-items-center bg-ink p-16 text-white lg:grid"><div className="max-w-lg"><Logo className="text-white"/><p className="mt-20 label">EDUCATION, ORCHESTRATED</p><h1 className="mt-5 text-5xl font-black leading-tight">One clear view of your entire institution.</h1><p className="mt-6 text-lg leading-8 text-white/60">Turn complex daily operations into simple, confident decisions for every learner.</p><div className="mt-12 rounded-3xl border border-white/10 bg-white/5 p-6"><ShieldCheck className="text-brand"/><p className="mt-4 leading-7 text-white/75">Passwords are hashed and sessions are stored securely in HTTP-only cookies.</p></div></div></section><section className="grid place-items-center p-5"><div className="w-full max-w-md"><div className="mb-10 lg:hidden"><Logo/></div><p className="label">{mode==="login"?"Welcome back":"Get started"}</p><h1 className="mt-2 text-3xl font-black">{mode==="login"?"Sign in to EduLedger":"Create your workspace"}</h1><p className="mt-2 text-muted">{mode==="login"?"Enter your registered account details.":"Your school workspace will be ready in moments."}</p><form onSubmit={submit} className="mt-8 space-y-4">{mode==="register"&&<><Field name="name" label="Full name" placeholder="Aarav Sharma" autoComplete="name"/><Field name="institution" label="Institution name" placeholder="Springfield Academy" autoComplete="organization"/></>}<Field name="email" label="Email address" placeholder="admin@school.edu" type="email" autoComplete="email"/><label className="block text-sm font-bold">Password<div className="relative mt-2"><input required minLength={8} maxLength={128} name="password" autoComplete={mode==="login"?"current-password":"new-password"} type={show?"text":"password"} className="w-full rounded-xl border bg-panel px-4 py-3 pr-12 outline-none focus:ring-2 focus:ring-brand" placeholder="At least 8 characters"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-muted" aria-label={show?"Hide password":"Show password"}>{show?<EyeOff/>:<Eye/>}</button></div></label>{mode==="register"&&<Field name="confirmPassword" label="Confirm password" placeholder="Repeat your password" type="password" autoComplete="new-password"/>}{mode==="login"&&<div className="text-right"><Link href="/forgot-password" className="text-sm font-bold text-brand">Forgot password?</Link></div>}{error&&<p role="alert" className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="btn-primary w-full disabled:cursor-wait disabled:opacity-70">{loading?<><Loader2 className="size-4 animate-spin"/>Please wait…</>:mode==="login"?"Sign in":"Create account"}</button></form><p className="mt-7 text-center text-sm text-muted">{mode==="login"?"New to EduLedger? ":"Already have an account? "}<Link className="font-bold text-brand" href={mode==="login"?"/register":"/login"}>{mode==="login"?"Create account":"Sign in"}</Link></p></div></section></main>}
function Field({name,label,placeholder,type="text",autoComplete}:{name:string;label:string;placeholder:string;type?:string;autoComplete?:string}){return <label className="block text-sm font-bold">{label}<input required name={name} type={type} autoComplete={autoComplete} placeholder={placeholder} className="mt-2 w-full rounded-xl border bg-panel px-4 py-3 font-normal outline-none focus:ring-2 focus:ring-brand"/></label>}
