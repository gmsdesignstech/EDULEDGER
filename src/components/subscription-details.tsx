import type{SubscriptionSummary}from"@/lib/db";import{formatInr}from"@/lib/subscription-plans";
export function SubscriptionDetails({subscription}:{subscription:SubscriptionSummary}){if(subscription.status==="UNPAID")return null;const rows=[["Status",subscription.status],["Selected plan",subscription.planName||"—"],["Student capacity",subscription.studentCapacity?.toLocaleString("en-IN")||"—"],["Current usage",`${subscription.studentUsage} active students`],["Amount",formatInr(subscription.amountPaid)],["Activation date",subscription.activationDate?new Date(subscription.activationDate).toLocaleDateString("en-IN"):"Pending"],["Expiry date",subscription.expiryDate?new Date(subscription.expiryDate).toLocaleDateString("en-IN"):"Pending"],["Razorpay order",subscription.razorpayOrderId||"—"],["Razorpay payment",subscription.razorpayPaymentId||"—"]];return <section className="mx-auto mb-16 max-w-5xl rounded-3xl border border-slate-200 bg-white p-5 shadow-sm sm:p-8"><h2 className="text-xl font-black">Subscription details</h2><p className="mt-1 text-sm text-slate-500">Current status and payment references for authorized school administrators.</p><dl className="mt-6 grid gap-px overflow-hidden rounded-2xl border border-slate-200 bg-slate-200 sm:grid-cols-2">{rows.map(([label,value])=><div className="min-w-0 bg-white p-4" key={label}><dt className="text-xs font-bold uppercase tracking-wider text-slate-500">{label}</dt><dd className="mt-1 break-all text-sm font-bold text-slate-800">{value}</dd></div>)}</dl></section>}
