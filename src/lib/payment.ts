import{createHmac,timingSafeEqual}from"crypto";
export function verifyRazorpaySignature(orderId:string,paymentId:string,signature:string,secret:string){const expected=createHmac("sha256",secret).update(`${orderId}|${paymentId}`).digest("hex");const a=Buffer.from(expected),b=Buffer.from(signature);return a.length===b.length&&timingSafeEqual(a,b)}
function credentials(){const key=process.env.RAZORPAY_KEY_ID,secret=process.env.RAZORPAY_KEY_SECRET;if(!key||!secret)throw new Error("Payment service is not configured");return{key,secret,authorization:`Basic ${Buffer.from(`${key}:${secret}`).toString("base64")}`};}
export async function createRazorpayOrder(input:{amount:number;receipt:string;notes:Record<string,string>}){const auth=credentials(),response=await fetch("https://api.razorpay.com/v1/orders",{method:"POST",headers:{Authorization:auth.authorization,"Content-Type":"application/json"},body:JSON.stringify({amount:input.amount,currency:"INR",receipt:input.receipt,notes:input.notes})});const data=await response.json()as{id?:string;amount?:number;currency?:string;error?:{description?:string}};if(!response.ok||!data.id)throw new Error(data.error?.description||"Could not create payment order");return{id:data.id,amount:Number(data.amount),currency:data.currency||"INR",key:auth.key};}
export async function getRazorpayPayment(paymentId:string){const auth=credentials(),response=await fetch(`https://api.razorpay.com/v1/payments/${encodeURIComponent(paymentId)}`,{headers:{Authorization:auth.authorization}}),data=await response.json()as{id?:string;order_id?:string;amount?:number;currency?:string;status?:string;captured?:boolean;error?:{description?:string}};if(!response.ok)throw new Error(data.error?.description||"Could not confirm payment");return data;}
