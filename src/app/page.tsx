import type{Metadata}from"next";import{LandingPage}from"@/components/landing-page";
export const metadata:Metadata={title:"EduLedger — School management, reimagined",description:"Manage admissions, attendance, fees, payroll, cashbook reporting and school operations in one beautiful platform."};
export default function Page(){return <LandingPage/>}
