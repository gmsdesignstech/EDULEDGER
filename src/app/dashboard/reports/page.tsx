import{dashboardData,getInstitution,listFees}from"@/lib/db";import{requireUser}from"@/lib/session";import{DataActions}from"@/components/data-actions";export const runtime="nodejs";const money=(n:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(n);export default async function Reports(){const user=await requireUser(),data=dashboardData(user.institutionId),school=getInstitution(user.institutionId),fees=listFees(user.institutionId);return <div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="label">Analytics</p><h1 className="mt-2 text-3xl font-black">Fees Report</h1><p className="mt-1 text-muted">Financial overview for academic year {school.academicYear}.</p></div><DataActions type="reports" exportType="reports"/></div><section className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{[["Total students",data.students],["Expected fees",money(data.fees.total)],["Collected",money(data.fees.collected)],["Pending",money(data.fees.pending)],["Overdue",money(data.fees.overdue)]].map(([k,v])=><div className="card p-5" key={k}><p className="text-sm text-muted">{k}</p><b className="mt-2 block text-2xl">{v}</b></div>)}</section><div className="card mt-5 overflow-hidden"><div className="border-b p-5"><h2 className="font-bold">Invoice status</h2></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-canvas text-xs uppercase text-muted"><tr><th className="px-5 py-4">Status</th><th className="px-5">Students</th><th className="px-5">Expected</th><th className="px-5">Collected</th><th className="px-5">Pending</th></tr></thead><tbody className="divide-y">{["Paid","Partially Paid","Pending","Overdue"].map(status=>{const rows=fees.filter(f=>f.status===status);return <tr key={status}><td className="px-5 py-4 font-bold">{status}</td><td className="px-5">{rows.length}</td><td className="px-5">{money(rows.reduce((a,f)=>a+Number(f.totalAmount),0))}</td><td className="px-5 text-emerald-600">{money(rows.reduce((a,f)=>a+Number(f.paidAmount),0))}</td><td className="px-5">{money(rows.reduce((a,f)=>a+Number(f.pendingAmount),0))}</td></tr>})}</tbody></table></div></div></div>}
