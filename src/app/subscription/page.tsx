import Link from"next/link";import{Logo}from"@/components/logo";import{ActivationPanel}from"@/components/subscription-activation";import{SubscriptionDetails}from"@/components/subscription-details";import{getSubscription}from"@/lib/db";import{requireUser}from"@/lib/session";import{SUBSCRIPTION_PLANS}from"@/lib/subscription-plans";
export const runtime="nodejs";
export default async function Subscription({searchParams}:{searchParams:Promise<{plan?:string}>}){const user=await requireUser(),subscription=getSubscription(user.institutionId),query=await searchParams,selected=SUBSCRIPTION_PLANS.some(plan=>plan.id===query.plan)?query.plan||"starter":subscription.planId||"starter";return <main className="min-h-screen overflow-x-hidden bg-white px-4 py-5 text-[#10203b] sm:px-8 sm:py-7"><div className="mx-auto max-w-6xl"><header className="flex items-center justify-between gap-3"><Logo/><Link href="/dashboard" className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold shadow-sm transition hover:border-blue-300 focus:outline-none focus:ring-2 focus:ring-blue-500">Back to dashboard</Link></header><ActivationPanel plans={SUBSCRIPTION_PLANS} initialPlanId={selected} subscription={subscription} schoolName={user.name}/><SubscriptionDetails subscription={subscription}/></div></main>}
