import {NextResponse} from "next/server";import{z}from"zod";import{attendanceRoster,markAttendance}from"@/lib/db";import{currentUser}from"@/lib/session";export const runtime="nodejs";
const schema=z.object({date:z.string().min(10),records:z.array(z.object({studentId:z.string().uuid(),status:z.enum(["Present","Absent","Late","Leave"]),remarks:z.string().max(300).optional()})).max(10000)});
export async function GET(request:Request){const user=await currentUser();if(!user)return NextResponse.json({error:"Unauthenticated"},{status:401});const q=new URL(request.url).searchParams,date=q.get("date"),className=q.get("class"),section=q.get("section");if(!date||!className||!section)return NextResponse.json({records:[]});return NextResponse.json({records:attendanceRoster(user.institutionId,date,className,section)});}
export async function POST(request:Request){const user=await currentUser();if(!user)return NextResponse.json({error:"Unauthenticated"},{status:401});if(!["SCHOOL_ADMIN","SUPER_ADMIN","TEACHER"].includes(user.role))return NextResponse.json({error:"Forbidden"},{status:403});const parsed=schema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Invalid attendance records"},{status:400});try{markAttendance(user.institutionId,parsed.data.date,parsed.data.records,user.name,user.id);return NextResponse.json({saved:parsed.data.records.length});}catch{return NextResponse.json({error:"Could not save attendance."},{status:400});}}
