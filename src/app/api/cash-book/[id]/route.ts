import { NextResponse } from "next/server";
import { currentUser } from "@/lib/session";
import { updateAccountTransaction,voidAccountTransaction } from "@/lib/db";
import { cashBookSchema } from "@/lib/account-validation";
export const runtime="nodejs";const roles=["SCHOOL_ADMIN","SUPER_ADMIN","ACCOUNTANT"];
export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){const user=await currentUser();if(!user)return NextResponse.json({error:"Unauthenticated"},{status:401});if(!roles.includes(user.role))return NextResponse.json({error:"Forbidden"},{status:403});const parsed=cashBookSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0]?.message||"Invalid transaction"},{status:400});try{return NextResponse.json({transaction:await updateAccountTransaction(user.institutionId,user.id,(await params).id,parsed.data)})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Could not update transaction"},{status:409})}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){const user=await currentUser();if(!user)return NextResponse.json({error:"Unauthenticated"},{status:401});if(!["SCHOOL_ADMIN","SUPER_ADMIN"].includes(user.role))return NextResponse.json({error:"Only administrators can void transactions."},{status:403});try{return NextResponse.json({transaction:await voidAccountTransaction(user.institutionId,user.id,(await params).id)})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Could not void transaction"},{status:409})}}
