import{NextResponse}from"next/server";import{z}from"zod";import{getInstitution,listAuditLogs,updateSettings}from"@/lib/db";import{currentUser}from"@/lib/session";export const runtime="nodejs";const schema=z.object({academicYear:z.string().min(4).max(20),address:z.string().max(500),phone:z.string().max(30),email:z.union([z.literal(""),z.string().email()])});export async function GET(){const user=await currentUser();return user?NextResponse.json({settings:getInstitution(user.institutionId),auditLogs:listAuditLogs(user.institutionId,50)}):NextResponse.json({error:"Unauthenticated"},{status:401});}export async function PUT(request:Request){const user=await currentUser();if(!user)return NextResponse.json({error:"Unauthenticated"},{status:401});if(!["SCHOOL_ADMIN","SUPER_ADMIN"].includes(user.role))return NextResponse.json({error:"Forbidden"},{status:403});const parsed=schema.safeParse(await request.json().catch(()=>null));return parsed.success?NextResponse.json({settings:updateSettings(user.institutionId,parsed.data,user.id)}):NextResponse.json({error:"Invalid settings"},{status:400});}
